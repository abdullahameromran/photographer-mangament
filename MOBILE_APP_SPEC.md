# Studio Flow — Mobile App Product & Engineering Specification

> مرجع تنفيذ تطبيق موبايل مطابق للنظام الحالي. التطبيق عربي RTL، ولا يحتوي على Landing Page أو صفحات تسويق. أول شاشة هي تسجيل الدخول، وبعده ينتقل المستخدم مباشرة إلى التطبيق أو شاشة الاشتراك.

## 1. الهدف والنطاق

تطبيق لإدارة استوديو تصوير يشمل الحجوزات، المواعيد، بيانات العملاء، الحسابات والمدفوعات، الطباعة، الفريق والصلاحيات، الإحصائيات، الإشعارات والاشتراك. يجب أن يستخدم نفس مشروع Supabase ونفس الجداول وسياسات RLS الموجودة في نسخة الويب، حتى تظهر البيانات نفسها لحظيًا في التطبيقين.

المنصات المقترحة: Flutter أو React Native. لا تنشئ Backend آخر، ولا تخزن بيانات العمل محليًا كمصدر أساسي. يمكن استخدام cache محلي للقراءة فقط وتحسين السرعة، لكن Supabase هو مصدر الحقيقة.

## 2. مبادئ التجربة

- اللغة الافتراضية العربية والاتجاه `RTL` في كل الشاشات.
- لا توجد Landing Page. المسار: Splash → Login/Signup → Subscription Guard → Main App.
- واجهة هادئة، احترافية، مناسبة لاستوديو تصوير، بألوان Slate الداكنة وBlue كلون رئيسي.
- Bottom Navigation لأهم الأقسام، مع More/Drawer للأقسام الإضافية.
- كل زر قابل للمس يجب ألا يقل عن `44×44` نقطة.
- الحقول المالية والمعلومات الحساسة لا تظهر إلا وفق صلاحيات المستخدم.
- حالات التحميل، الفراغ، الخطأ، وفقد الاتصال مطلوبة في كل قائمة.
- استخدم Skeleton loading للقوائم وSpinner للعمليات القصيرة.
- استخدم Toast/Snackbar للنجاح والفشل، وConfirmation Dialog قبل الحذف أو الإيقاف.

## 3. الهوية البصرية

### الألوان

| الاستخدام | اللون | Hex تقريبي |
|---|---:|---:|
| الخلفية العامة | Slate 100 | `#F1F5F9` |
| خلفية البطاقات | White | `#FFFFFF` |
| الهيدر/الشريط الداكن | Slate 900 | `#0F172A` |
| الخلفية الأغمق | Slate 950 | `#020617` |
| النص الأساسي | Slate 900 | `#0F172A` |
| النص الثانوي | Slate 500 | `#64748B` |
| الحدود | Slate 200 | `#E2E8F0` |
| اللون الرئيسي | Blue 600 | `#2563EB` |
| Blue hover/pressed | Blue 700 | `#1D4ED8` |
| نجاح/مدفوع | Emerald 500/600 | `#10B981` / `#059669` |
| تحذير/متبقي | Amber 400/500/600 | `#FBBF24` / `#F59E0B` / `#D97706` |
| خطأ/إلغاء | Rose 500/600 | `#F43F5E` / `#E11D48` |
| اشتراك/تفاصيل | Violet 600 | `#7C3AED` |
| Facebook | Facebook Blue | `#1877F2` |

### الخطوط والأبعاد

- الخط: `Cairo` أولًا، ثم `Tajawal`، ثم system sans-serif.
- العنوان الكبير: 28–32، وزن 800/900.
- عنوان الصفحة: 22–26، وزن 800/900.
- عنوان البطاقة: 15–17، وزن 700/800.
- النص العادي: 13–15.
- النص المساعد: 11–12.
- نصف قطر البطاقات: 16–24.
- نصف قطر الحقول والأزرار: 10–14.
- المسافة القياسية: 4، 8، 12، 16، 20، 24، 32.
- ظلال خفيفة فقط؛ الحدود أهم من الظلال.

## 4. بنية التنقل

### Bottom Navigation

1. **الحجوزات** — كل الحجوزات.
2. **القادم** — اليوم والثلاثة أيام التالية.
3. **التقويم** — عرض شهري/يومي.
4. **حسابات العملاء** — دفع كام وعليه كام.
5. **المزيد** — Bottom Sheet أو Drawer.

### قائمة «المزيد»

- إدارة الطباعة.
- التقارير والإحصائيات.
- الاشتراك والباقات.
- المستخدمون والصلاحيات — يظهر للمدير فقط.
- حسابي.
- تسجيل الخروج.

احتفظ بالتبويب النشط وحالة الفلاتر أثناء التنقل داخل الجلسة.

## 5. المصادقة وبدء التطبيق

### Splash

- شعار كاميرا + `Studio Flow`.
- افحص جلسة Supabase المخزنة.
- حدّث الجلسة تلقائيًا عند الحاجة.
- لا تعرض بيانات التطبيق قبل انتهاء فحص الجلسة والاشتراك.

### تسجيل الدخول

الحقول: البريد الإلكتروني، كلمة المرور، إظهار/إخفاء كلمة المرور. استخدم Supabase Auth password sign-in. عند النجاح افحص الاشتراك الفعلي ثم افتح التطبيق.

### إنشاء حساب

الحقول: الاسم بالكامل، البريد الإلكتروني، كلمة المرور (6 أحرف على الأقل). التسجيل الذاتي لا يمنح اشتراكًا تلقائيًا. بعد التسجيل:

- إن كان تأكيد البريد مفعّلًا: اطلب تأكيد البريد ثم تسجيل الدخول.
- بعد الدخول بدون اشتراك: اعرض شاشة «حسابك بانتظار التفعيل».
- يظهر الحساب في لوحة Super Admin ضمن طلبات التسجيل الجديدة.

### تسجيل الخروج

يجب أن يكون متاحًا من صفحة الحساب ومن شاشة انتظار/انتهاء الاشتراك. يمسح Supabase session وcache الحساس ثم يعيد إلى Login.

## 6. حارس الاشتراك Subscription Guard

لا تفحص اشتراك `user_id` الحالي فقط. استدعِ RPC:

```text
current_effective_subscription()
```

المنطق:

1. إذا كان المستخدم مالكًا ولديه اشتراك، استخدم اشتراكه.
2. إذا كان متعاونًا، استخدم اشتراك مالك `studio_id` نفسه.
3. الاشتراك صالح فقط إذا `enabled = true` و`expires_at > now()`.
4. انتهاء اشتراك المالك يمنع كل أعضاء الاستوديو.
5. تجديده يعيد الوصول للجميع.

شاشة عدم الاشتراك تحتوي على الحالة، زر التواصل مع الإدارة عبر Facebook، زر الاتصال، وزر واضح لتسجيل الخروج.

Facebook Admin Page:

```text
https://www.facebook.com/profile.php?id=61593185351334
```

رقم التواصل الحالي:

```text
201554670453
```

## 7. نموذج الصلاحيات

كل مستخدم لديه:

- `profiles`: الاسم، الوظيفة، حالة الحساب، هل هو مدير، والاستوديو.
- `user_permissions`: صلاحيات الأفعال ونطاق الحجوزات.
- `user_field_permissions`: عرض/تعديل كل حقل.
- `user_selected_bookings`: الحجوزات المحددة إذا كان النطاق selected.

### نطاق الحجوزات

- `all`: كل حجوزات الاستوديو.
- `assigned_only`: الحجوزات المسندة للمستخدم فقط.
- `selected`: قائمة حجوزات يحددها المدير.

### صلاحيات الأفعال

- عرض الحجز.
- إنشاء حجز.
- تعديل حجز.
- حذف حجز.
- تغيير الحالة.
- إضافة ملاحظات.

### الحقول القابلة للتحكم

اسم العميل، الهاتف، واتساب، عنوان/نوع الحجز، التاريخ، الوقت، المكان، السعر، العربون/المدفوع، المتبقي وحالة الدفع، الطباعة، الملاحظات، التذكير، الفريق المسند، حالة الحجز.

طبّق الإخفاء في الواجهة، لكن لا تعتمد عليه للحماية؛ RLS ودوال قاعدة البيانات هي الحماية الفعلية.

## 8. الشاشات الوظيفية

### 8.1 كل الحجوزات

- بحث بالعميل، الهاتف، العنوان، والمكان.
- فلاتر: الكل، مدفوع بالكامل، غير مدفوع بالكامل، نوع الحجز.
- بطاقة الحجز: العنوان، العميل، التاريخ والوقت، المكان، الأنواع، الحالة، السعر/المدفوع/المتبقي حسب الصلاحية، أعضاء الفريق والطباعة.
- إجراءات وفق الصلاحيات: عرض، تعديل، حذف، تغيير الحالة.
- زر Floating Action «حجز جديد» إن كان مسموحًا.

### 8.2 إنشاء/تعديل الحجز

#### بيانات العميل

- اسم العميل: مطلوب، حرفان على الأقل.
- رقم الموبايل: اختياري، وإن أُدخل يجب أن يكون رقمًا مصريًا من 11 رقمًا ويبدأ `010/011/012/015`.
- واتساب يساوي الهاتف افتراضيًا.

#### تفاصيل الحجز

- أنواع متعددة: سيشن، قاعة، حنة، شبكة، كتب كتاب، بارتي، Wedding، أخرى.
- إمكانية إعطاء كل نوع تاريخًا ووقتًا مختلفًا عبر `booking_type_schedules`.
- تاريخ الحجز مطلوب.
- وقت البداية والنهاية مطلوبان.
- المكان ورابط Google Maps اختياريان.
- رابط الخريطة يجب أن يبدأ بـ `http://` أو `https://`.

#### الحجوزات الليلية

- تاريخ الحجز هو **تاريخ البداية دائمًا**.
- إذا كان وقت النهاية أقل من وقت البداية، فالنهاية في اليوم التالي.
- مثال: `18:00 → 05:00` يعني من السادسة مساءً في التاريخ المختار إلى الخامسة صباح اليوم التالي.
- يظل الحجز محسوبًا في القوائم والتقارير ضمن تاريخ بدايته.
- ارفض فقط تساوي البداية والنهاية.
- اعرض للمستخدم تنبيهًا: «ينتهي صباح اليوم التالي، وسيظل الحجز محسوبًا ضمن تاريخ بدايته».

#### الحسابات

- السعر `>= 0`.
- هل دفع عربون؟
- المدفوع `>= 0` ولا يتجاوز السعر.
- المتبقي = `max(0, price - paid)`.
- حالة الدفع:
  - `not_paid`: المدفوع صفر.
  - `partial`: أكبر من صفر وأقل من السعر.
  - `paid`: المدفوع يساوي/يتجاوز السعر والسعر أكبر من صفر.
- إضافات حرة داخل الحجز: اسم يكتبه المستخدم، كمية، وسعر؛ مثل برومو، مصور إضافي أو درون.
- السعر الرئيسي يظل إجمالي الاتفاق النهائي مع العميل، ويجب أن يتضمن قيمة الإضافات.
- مصاريف داخلية حرة: الاسم والمبلغ؛ مثل مصور خارجي، الطباعة والمواصلات.
- إجمالي المصروفات وصافي الربح لا يظهران إلا بصلاحية `expenses` المستقلة.
- رفع صورة إثبات العربون اختياري إلى bucket باسم `deposit-receipts`.

#### الطباعة

- هل توجد طباعة؟
- تابلوه كبير، تابلوه صغير، ألبوم 30×45، ألبوم 30×60، صور كروت وعددها.
- حالة الطباعة: لم تبدأ، جاري التجهيز، جاهزة، تم التسليم.
- منتجات طباعة/تسليم مخصصة بلا قائمة ثابتة: اسم أو مقاس يكتبه المستخدم، كمية وتكلفة؛ مثل 30×80 أو بوكس عميل.

#### الإدارة

- التذكير: قبل ساعة، ساعتين، 3، 6، 12، يوم، أو مخصص.
- إسناد الحجز لأكثر من عضو.
- حالة الحجز: جديد، في انتظار العربون، مؤكد، قادم، تم التصوير، جاري التجهيز، جاهز، تم التسليم، ملغي.

### 8.3 اليوم والثلاثة أيام القادمة

- مجموعات حسب اليوم.
- إجمالي حجوزات اليوم والمدفوع والمتبقي.
- ترتيب زمني.
- إجراءات سريعة: اتصال، واتساب للعميل، خريطة، تعديل، تغيير الحالة.
- الحجز الليلي يظهر تحت يوم بدايته.

### 8.4 التقويم

- شهر حالي مع السابق/التالي والعودة لليوم.
- Badge بعدد الحجوزات لكل يوم.
- الضغط على يوم يعرض قائمة اليوم.
- بحث وفلاتر الحالة.
- الضغط على مساحة اليوم يفتح إنشاء حجز بالتاريخ المحدد.

### 8.5 حسابات العملاء

جمّع حجوزات العميل بواسطة رقم الهاتف بعد إزالة الرموز. إذا لم يوجد هاتف، استخدم الاسم الموحّد. اعرض:

- عدد العملاء.
- إجمالي الاتفاقات.
- إجمالي المدفوع.
- إجمالي المتبقي.
- نسبة التحصيل.
- لكل عميل: الاسم، الهاتف، عدد الحجوزات، آخر موعد، الإجمالي، دفع كام، عليه كام، حالة السداد ونسبة التحصيل.
- بحث بالاسم/الهاتف.
- فلاتر: الكل، عليهم متبقي، تم السداد.
- ترتيب: الأعلى متبقيًا، الأعلى دفعًا، الأعلى قيمة، الاسم.

### 8.6 إدارة الطباعة

- اعرض الحجوزات التي `has_printing = true`.
- Kanban أو Tabs حسب حالة الطباعة.
- تفاصيل المنتجات المطلوبة وعدد صور الكروت.
- بحث بالعنوان/العميل.
- تغيير الحالة وفتح تفاصيل الحجز.

### 8.7 التقارير

- عدد الحجوزات.
- إجمالي الإيراد المتفق عليه.
- إجمالي المدفوع.
- إجمالي المتبقي.
- نسبة التحصيل.
- عدد انتظار العربون.
- توزيع حالات الحجوزات والأنواع والطباعة.
- لا تعرض أرقامًا مالية إذا لم تسمح field permissions.

### 8.8 المستخدمون والصلاحيات

للمدير فقط:

- قائمة أعضاء نفس الاستوديو فقط.
- إضافة متعاون: الاسم، البريد، كلمة مرور مؤقتة 8 أحرف على الأقل، الدور.
- Edge Function: `create-collaborator`.
- الدالة تنشئ Auth user مؤكدًا، تنسخ `studio_id` للمدير، وتجعله عضوًا غير Admin.
- المتعاون يرث اشتراك مالك الاستوديو ولا يشتري اشتراكًا منفصلًا.
- تعديل الحالة والدور ونطاق الحجوزات وصلاحيات الأفعال والحقول.
- حذف الملف وفق السياسات الحالية.

### 8.9 الاشتراك والباقات

- حالة الاشتراك: نشط/منتهي/موقوف.
- الباقة: trial، monthly، quarterly، yearly.
- السعر والمدة.
- مالك الاشتراك.
- تاريخ البداية والنهاية.
- الأيام والنسبة المتبقية.
- ملاحظات الإدارة.
- توضيح أن الاشتراك يشمل جميع أعضاء الاستوديو.
- Facebook + اتصال للتجديد.

### 8.10 حسابي

- الصورة، الاسم، البريد، الدور، حالة الحساب.
- تغيير كلمة المرور.
- تسجيل الخروج.
- لا تسمح للمستخدم بترقية نفسه أو تغيير `studio_id` أو `is_admin`.

## 9. Super Admin Mobile (اختياري داخل نفس التطبيق)

يفتح فقط لحساب موجود في `super_admins`. لا تعتمد على البريد وحده. الوظائف:

- أعداد كل المشتركين، النشطين، والمنتهين/الموقوفين.
- طلبات التسجيل الجديدة بدون subscription.
- اختيار باقة ثم تفعيل الطلب عبر `approve_signup`.
- إنشاء مشترك مباشرة عبر Edge Function `create-subscriber`.
- تجديد، تفعيل، وإيقاف الاشتراك.
- زر «عرض التفاصيل» لكل مشترك عبر `get_subscriber_details`.
- Popup/Full Screen يعرض بيانات الحساب والاستوديو والاشتراك، عدد الحجوزات، الإجماليات المالية، وكل مواعيد وحجوزات الاستوديو.

## 10. Supabase Database

استخدم الترحيلات داخل `supabase/migrations` بالترتيب من `01` إلى `16`. لا تعِد إنشاء Schema مختلف.

### الجداول الأساسية

| الجدول | الغرض وأهم الحقول |
|---|---|
| `profiles` | `id`, `full_name`, `email`, `phone`, `job_title`, `is_admin`, `status`, `studio_id`, timestamps |
| `studios` | `id`, `name`, `owner_id`, `created_at` |
| `bookings` | العميل، الاتصال، العنوان، الأنواع، التاريخ، الوقت، المكان، السعر، المدفوع، المتبقي المحسوب، حالة الدفع، حالة الحجز، `studio_id`, `created_by` |
| `booking_type_schedules` | موعد منفصل لكل نوع داخل الحجز |
| `booking_printing` | خيارات وحالة الطباعة |
| `booking_reminders` | نوع وموعد التذكير وحالة الإرسال |
| `booking_assignees` | ربط متعدد بين الحجوزات والمستخدمين |
| `user_permissions` | صلاحيات الأفعال ونطاق الحجز |
| `user_field_permissions` | عرض/تعديل الحقول |
| `user_selected_bookings` | حجوزات النطاق المحدد |
| `field_catalog` | كتالوج الحقول القابلة للتحكم |
| `subscriptions` | `user_id`, `plan_code`, `starts_at`, `expires_at`, `enabled`, `notes` |
| `super_admins` | معرفات مديري المنصة |

### Enums

```text
user_status: active, disabled
booking_access_scope: all, assigned_only, selected
payment_status: not_paid, partial, paid
printing_status: not_started, preparing, ready, delivered
booking_status: new, waiting_deposit, confirmed, upcoming,
                photographed, preparing, ready, delivered, cancelled
booking_type: session, hall, henna, shabaka, katb_ketab,
              party, wedding, other
reminder_type: 1h, 2h, 3h, 6h, 12h, 1d, custom
subscription plan_code: trial, monthly, quarterly, yearly
```

### RPC المستخدمة

| RPC | الاستخدام |
|---|---|
| `current_effective_subscription()` | اشتراك المستخدم أو مالك الاستوديو |
| `list_pending_signups()` | طلبات التسجيل للـSuper Admin |
| `approve_signup(p_user, p_plan)` | إنشاء الاشتراك وتجهيز الاستوديو والصلاحيات |
| `get_subscriber_details(p_user)` | تفاصيل المشترك وحجوزات استوديوه للـSuper Admin |
| `is_super_admin()` | تحقق صلاحية إدارة المنصة |

### Edge Functions

| Function | الاستخدام |
|---|---|
| `create-subscriber` | إنشاء Auth user + studio + owner + subscription بواسطة Super Admin |
| `create-collaborator` | إنشاء عضو داخل studio المدير الحالي |
| `send-reminder` | معالجة/إرسال التذكيرات حسب الإعداد الحالي |

### Storage

- Bucket: `deposit-receipts`.
- المسار المقترح: `{auth_user_id}/{uuid}.{extension}`.
- اسمح بالصور فقط، حد حجم مناسب مثل 5MB، واضغط الصورة قبل الرفع على الموبايل.

## 11. Multi-Tenancy وRLS

- كل بيانات العمل مرتبطة بـ `studio_id`.
- المستخدم يرى استوديوه فقط.
- لا ترسل `studio_id` من واجهة يمكن التلاعب بها إن أمكن؛ دعه يُحدد من `current_studio_id()`/trigger.
- Super Admin لا يقرأ حجوزات الاستوديوهات عبر query عام؛ يستخدم RPC المحمية.
- لا تستخدم Service Role Key داخل تطبيق الموبايل مطلقًا.
- استخدم Anon Key فقط مع جلسة المستخدم.
- إنشاء Auth users الآخرين يتم فقط عبر Edge Functions التي تحتفظ بـService Role على الخادم.

## 12. Queries وRealtime

- بعد تسجيل الدخول حمّل profile + permissions + effective subscription.
- اجلب الحجوزات مع printing، assignees، reminders، وtype schedules.
- رتّب الحجوزات بتاريخ البداية.
- فعّل Supabase Realtime للحجوزات والطباعة إن كان متاحًا، ثم أعد query عند insert/update/delete بدل تركيب بيانات ناقصة.
- استخدم pull-to-refresh في كل القوائم.
- عند mutation استخدم optimistic UI فقط مع rollback واضح عند الفشل.

## 13. الإشعارات

- اطلب Notification permission بعد دخول التطبيق، وليس في شاشة Login.
- خزّن Device Push Token في جدول مخصص مرتبط بالمستخدم والاستوديو إذا أضيف Push backend.
- احترم `booking_reminders.remind_at`.
- عند الضغط على الإشعار افتح تفاصيل الحجز مباشرة.
- امنع تكرار الإشعار باستخدام معرف الحجز + موعد التذكير.

## 14. قواعد التنسيق

- العملة: `Intl/NumberFormat ar-EG`, currency `EGP`, بدون كسور افتراضيًا.
- التاريخ: locale `ar-EG`.
- الوقت: اعرض 12 ساعة بالعربية، واحفظ `HH:mm:ss`/Postgres time.
- الهاتف في العرض `LTR` حتى داخل واجهة RTL.
- الروابط الخارجية تُفتح خارج التطبيق أو داخل in-app browser واضح.
- WhatsApp للعملاء: حوّل الرقم المصري الذي يبدأ بـ0 إلى `20...`.

## 15. حالات الخطأ المهمة

- Session expired: حاول refresh مرة واحدة، ثم Logout.
- لا يوجد اشتراك: Pending screen، وليس خطأ عام.
- اشتراك منتهي/موقوف: Renewal screen.
- Permission denied: رسالة عربية ولا تعرض زرًا لا يستطيع المستخدم استخدامه.
- Upload failed: احتفظ بالنموذج ولا تغلقه.
- Create/update failed: rollback واظهر سبب Supabase المرسل.
- Offline: اعرض آخر cache مع علامة «غير متصل»، وامنع العمليات التي قد تتكرر دون queue موثوق.
- RPC غير موجودة: رسالة واضحة باسم migration المطلوبة؛ لا تحوّلها إلى «غير مصرح».

## 16. معايير القبول

- المستخدم والويب يرون نفس البيانات فور التحديث.
- لا يستطيع مستخدم رؤية بيانات استوديو آخر بتغيير query أو ID.
- المتعاون يدخل باستخدام اشتراك المالك.
- الحجز `18:00 → 05:00` يُحفظ ويظهر ضمن يوم البداية.
- الحسابات المالية متطابقة مع الويب.
- الصلاحيات تخفي وتمنع التعديل الفعلي.
- التسجيل الجديد يظهر للـSuper Admin ويمكن تفعيله.
- زر Logout يعمل حتى عند عدم وجود اشتراك.
- التطبيق يعمل RTL بالكامل على شاشات صغيرة وكبيرة.
- لا توجد Landing Page أو Pricing marketing قبل Login.

## 17. ترتيب التنفيذ المقترح

1. Theme وRTL وSupabase client وsecure session storage.
2. Auth + Subscription Guard.
3. Profile/permissions/studio bootstrap.
4. Bookings list + details + create/edit.
5. Upcoming + Calendar.
6. Client accounts + Printing + Stats.
7. Users/permissions + shared subscription.
8. Subscription page + Account page.
9. Notifications and uploads.
10. Super Admin screens إن كانت مطلوبة في تطبيق الموبايل.
11. اختبارات RLS، الصلاحيات، الحجوزات الليلية، وتجارب الأجهزة.

## 18. الملفات المرجعية في نسخة الويب

- `src/types.ts` — الأنواع وحالات النظام.
- `src/lib/supabase.ts` — التحويل بين العربية وقيم DB والـAPIs.
- `src/utils/permissions.ts` — الحسابات والصلاحيات والتنسيق.
- `src/components/BookingModal.tsx` — نموذج الحجز والتحقق.
- `src/components/ClientAccounts.tsx` — تجميع حسابات العملاء.
- `src/components/SubscriptionPage.tsx` — صفحة الاشتراك.
- `src/components/SuperAdminPage.tsx` — إدارة المشتركين.
- `supabase/migrations/*.sql` — المصدر النهائي للـSchema وRLS والدوال.

هذا المستند يصف السلوك الحالي المطلوب، لكن عند التعارض التقني يكون ترتيب المصدر: **آخر migrations → منطق `supabase.ts` → `types.ts` → هذا المستند**.
